namespace: Tests

paths:
    tests: tests
    data: tests/_data
    support: tests/_support
    envs: tests/_envs
    output: tests/_output

suites:
    acceptance:
        path: acceptance
        actor: AcceptanceTester
        modules:
            enabled:
                - Asserts
                - PhpBrowser:
                    url: http://nginx:80/
        coverage:
            enabled: false
    unit:
        path: unit
        actor: UnitTester
        modules:
            enabled:
                - Asserts

settings:
    colors: true
    memory_limit: 1024M
    shuffle: true
    lint: true

modules:
    config:
        Db:
            dsn: ''
            user: ''
            password: ''
            dump: tests/_data/dump.sql

coverage:
    enabled: true
    whitelist:
        include:
            - php/*
